# TODO - Fix settings/company 500 error

- [x] Update backend `PUT /api/settings/company` to sanitize and normalize payload
- [x] Update frontend company settings page to send JSON-only payload (no multipart FormData)
- [x] Run quick backend syntax check
- [x] Mark all tasks completed after verification
